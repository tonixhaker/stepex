const prefix = 'TRANSLATOR_';

const TRANSLATE = `${prefix}TRANSLATE`;
const SET_FROM_LANG = `${prefix}SET_FROM_LANG`;
const SET_TO_LANG = `${prefix}SET_TO_LANG`;
const SET_FROM_TEXT = `${prefix}SET_FROM_TEXT`;
const LANGS = `${prefix}LANGS`;

export {
    TRANSLATE,
    SET_FROM_LANG,
    SET_TO_LANG,
    SET_FROM_TEXT,
    LANGS
}