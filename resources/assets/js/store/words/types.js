const prefix = 'WORDS_';

const GET = `${prefix}GET`;
const PAGINATION = `${prefix}PAGINATION`;
const PAGINATION_LIST = `${prefix}PAGINATION_LIST`;

export {
    GET,
    PAGINATION,
    PAGINATION_LIST
}